[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "neu_perm"
version = "1.0.0"
description = "NeuPerm: disrupting malware hidden in neural-network parameters via permutation symmetry"