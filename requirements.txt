# NeuPerm runtime dependencies (loose ranges).
# For an exact, reproducible environment use requirements-lock.txt, or the
# conda spec in environment.yaml (Python 3.9.18, CUDA 11.8). See README.md.
#
# Install the CUDA 11.8 PyTorch wheels, e.g.:
#   pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 \
#     --index-url https://download.pytorch.org/whl/cu118

torch>=2.1,<2.2
torchvision>=0.16,<0.17
torchaudio>=2.1,<2.2
numpy>=1.25,<1.26
scipy>=1.9,<1.10
scikit-learn>=1.3,<1.4
pandas>=2.0,<2.1
pillow>=10.0,<10.1
tqdm>=4.66

# HuggingFace / experiment stack (used by the LLM and SNR experiments)
transformers>=4.45,<4.49
tokenizers>=0.20
safetensors>=0.4.1
huggingface-hub>=0.24,<1.0
datasets>=2.16,<3.0
accelerate>=0.28
evaluate>=0.4
psutil>=6.0
